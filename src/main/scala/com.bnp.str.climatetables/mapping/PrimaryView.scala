package com.bnp.str.climatetables.mapping

/* TODO[EXTRACTION] lines 3-6 are a COLLAPSED import fold in the IDE, rendered
   as `> import ...`. Four import lines are hidden. Only one is inferable from
   the body (LoggerFactory on line 10); the other three are unknown.
   Re-photograph with the fold expanded to capture them verbatim. */
import org.slf4j.LoggerFactory
/* ⟪CUT⟫ 3 further import lines, not recoverable from the body */

object PrimaryView {

  private val log = LoggerFactory.getLogger(this.getClass)

}
